# Compiled source #
###################
*.o
*.a
*.qm

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.log

# Intermediary build files #
############################
.qmake.stash
android-build/
android-PortaBase-deployment-settings.json
/build/
_build/
/doc/html/
config.h
config.status
Makefile*
metakit/builds/*.exe
metakit/builds/demo
metakit/builds/dump
metakit/builds/myio
metakit/unix/autom4te.cache/
object_script.Portabase.Debug
object_script.PortaBase.Release
packaging/android/apk_template/libs/
portabase.pro.user
resources/help/translations/templates/.doctrees/

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor and IDE artifacts #
############################
*~


# APM dependencies
apm_modules/

# Files APM deploys into .claude/ (pinned/reproducible via apm.lock.yaml,
# not vendored -- see apm.yml). Update this list by hand when apm.yml
# changes; check apm.lock.yaml's deployed_files for the current set.
/.claude/commands/arrow-maintenance.md
/.claude/commands/map-codebase.md
/.claude/skills/arrow-maintenance/
/.claude/skills/lid-coach/
/.claude/skills/linked-intent-dev/
/.claude/skills/map-codebase/
/.claude/skills/update-lid/

# External repos cloned for reference (see docs/reference-repos.yaml)
/.reference-repos/

# Local mirror of GitHub issues/PRs for agent context (see CLAUDE.md, `mise run issues-sync`)
/.issues/

# mdBook HTML output (local browsing only, see `mise run mdbook-build`)
/docs/book/
