diff -aur metakit-2.4.8/include/mk4.h metakit-2.4.8-patched/include/mk4.h
--- metakit-2.4.8/include/mk4.h	2002-11-02 17:29:13.000000000 -0500
+++ metakit-2.4.8-patched/include/mk4.h	2002-11-30 03:21:16.000000000 -0500
@@ -319,6 +319,7 @@
   friend bool operator> (const c4_View&, const c4_View&);
   friend bool operator<= (const c4_View&, const c4_View&);
   friend bool operator>= (const c4_View&, const c4_View&);
+  static bool _CaseSensitive;
   
 protected:
   void _IncSeqRef();
diff -aur metakit-2.4.8/python/PyStorage.cpp metakit-2.4.8-patched/python/PyStorage.cpp
--- metakit-2.4.8/python/PyStorage.cpp	2002-11-02 17:29:13.000000000 -0500
+++ metakit-2.4.8-patched/python/PyStorage.cpp	2002-12-01 14:48:42.000000000 -0500
@@ -519,6 +519,23 @@
   catch (...) { return 0; }
 }
 
+static char* ignorecase__doc = 
+"ignorecase(flag) -- use case sensitive comparisons in all view methods if flag is 0, case insensitive if flag is 1";
+
+static PyObject* PyView_ignorecase(PyObject* o, PyObject* _args) {
+  try {
+    PWOSequence args(_args);
+    int flag = PWONumber(args[0]);
+    if (flag == 0)
+      c4_View::_CaseSensitive = true;
+    else
+      c4_View::_CaseSensitive = false;
+    Py_INCREF(Py_None);
+    return Py_None;
+  }
+  catch (...) { return 0; }
+}
+
 static PyMethodDef Mk4Methods[] = {
   {"view", PyView_new, METH_VARARGS, "view() - create a new unattached view"},
   {"storage", PyStorage_new, METH_VARARGS, storage__doc},
@@ -526,6 +543,7 @@
     "property(type, name) -- create a property of given type and name"},
   {"wrap", PyView_wrap, METH_VARARGS, 
     "wrap(seq,props,usetuples=0) - wrap a sequence as a read-only view"},
+  {"ignorecase", PyView_ignorecase, METH_VARARGS, ignorecase__doc},
   {0, 0, 0, 0}
 };
 
diff -aur metakit-2.4.8/src/format.cpp metakit-2.4.8-patched/src/format.cpp
--- metakit-2.4.8/src/format.cpp	2002-05-05 16:29:14.000000000 -0400
+++ metakit-2.4.8-patched/src/format.cpp	2002-11-30 03:22:58.000000000 -0500
@@ -921,7 +921,10 @@
   c4_String v1 ((const char*) b1_.Contents(), b1_.Size());
   c4_String v2 ((const char*) b2_.Contents(), b2_.Size());
   
-  return v1.CompareNoCase(v2);
+  if (c4_View::_CaseSensitive)
+    return v1.Compare(v2);
+  else
+    return v1.CompareNoCase(v2);
 }
 
 void c4_FormatS::Insert(int index_, const c4_Bytes& buf_, int count_)
diff -aur metakit-2.4.8/src/view.cpp metakit-2.4.8-patched/src/view.cpp
--- metakit-2.4.8/src/view.cpp	2002-10-27 07:02:10.000000000 -0500
+++ metakit-2.4.8-patched/src/view.cpp	2002-11-30 03:22:15.000000000 -0500
@@ -1027,6 +1027,8 @@
   return na == nb ? 0 : i < na ? +1 : -1;
 }
 
+bool c4_View::_CaseSensitive = false;
+
 /////////////////////////////////////////////////////////////////////////////
 
 /** @class c4_Cursor
diff -aur metakit-2.4.8/unix/Makefile.in metakit-2.4.8-patched/unix/Makefile.in
--- metakit-2.4.8/unix/Makefile.in	2002-11-02 17:29:13.000000000 -0500
+++ metakit-2.4.8-patched/unix/Makefile.in	2002-11-30 03:17:55.000000000 -0500
@@ -116,7 +116,7 @@
 
 install-python: Mk4py@SHLIB_SUFFIX@
 	$(INSTALL_PROGRAM) Mk4py@SHLIB_SUFFIX@ $(DESTDIR)$(pylibdir)
-	$(INSTALL_PROGRAM) $(srcdir)/../python/metakit.py $$(DESTDIR)(pylibdir)
+	$(INSTALL_PROGRAM) $(srcdir)/../python/metakit.py $(DESTDIR)$(pylibdir)
 
 clean:
 	$L rm -f *.la *.o *.lo
