File properties dialog (size, rows, etc.)
Option to hide navigation bar (and show all rows)
Pie chart of enum options in summary dialog
Type letter to go to rows whose first text field in the view starts with it
